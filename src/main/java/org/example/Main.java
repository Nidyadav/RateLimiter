package org.example;

//TIP To <b>Run</b> code, press <shortcut actionId="Run"/> or
// click the <icon src="AllIcons.Actions.Execute"/> icon in the gutter.
public class Main {
    static void main() {

        UserSlidingWindow userSlidingWindow = new UserSlidingWindow(1);
        userSlidingWindow.accessApp(1);
        userSlidingWindow.accessApp(1);
        userSlidingWindow.accessApp(1);
        userSlidingWindow.accessApp(1);

    }
}
